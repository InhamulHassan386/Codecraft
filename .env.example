# MySQL connection used by the API server
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=change-me
DB_NAME=codecraft
PORT=8787
# Local admin bootstrap values; keep these in .env only, never commit .env
ADMIN_NAME=Inhamul Hassan
ADMIN_EMAIL=inhamulhassan@664gmail.com
ADMIN_PASSWORD=hassan55
# Set this in production if the API is hosted separately
# VITE_API_URL=https://api.example.com/api
